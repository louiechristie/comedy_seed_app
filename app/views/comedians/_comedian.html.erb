<div class="comedian">
  <%= link_to comedian do %>
    <%= image_tag comedian.image.url || "silhouette.svg" %>
    <div class="name_and_rating">
      <%= comedian.try :stage_name %>
      <%= comedian.avg_rating.nan? ? "" : comedian.avg_rating.to_s+"*"  %>
    </div>
   <% end %>
</div>